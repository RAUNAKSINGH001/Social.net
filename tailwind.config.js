export const content = [
  "./index.html",
  "./src/**/*.{js,ts,jsx,tsx}", // include all component files
];
export const darkMode = 'class';
export const theme = {
  extend: {
    // Optional: extend custom styles
  },
};
export const plugins = [];
